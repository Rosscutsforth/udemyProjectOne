using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BooleanLogicChallenge : MonoBehaviour
{

    bool test = "a" == "a";

    // Start is called before the first frame update
    void Start()
    {
        print(test);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
